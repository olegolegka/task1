print("Генератор паролей")

number = int(input("Введите число от 1 до 9"))

if number >= 1 and number <=5:
    password = number * (1+2+3+4+5)
    password += 15 * number
    password *= 1*2*3*4*5
elif number >=6 and number <=9:
    password = number * (6+7+8+9)
    password *=5
    password /= 126
print(password)
